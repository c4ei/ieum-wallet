# Contributing

1. Create work from `dev` and open a pull request into `dev`.
2. Release changes move from `dev` to `main` through a reviewed pull request.
3. Never commit private keys, credentials, `.env` files, database dumps or production logs.
4. Run the repository tests and build checks before requesting review.
